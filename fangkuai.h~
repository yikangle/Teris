#include<stdlib.h>
#include<stdio.h>
#include<string.h>

typedef enum shapes{
left_L=1,down_L,right_L,up_L,
v_N,h_N;
v_I,h_I;
up_T,left_T,down_T,right_T,
O

}Shapes


typedef struct cube *pcube;
struct cube {
LIST cube_list[13];
int curr_x,curr_y;
int index;



}
typedef struct lcube Lcube;
struct lcube{
int x,y;//第一个点的坐标
Shapes which;//哪个图形 枚举




}
enum keys {
LEFT;
RIGHT;
UP;
DOWN;
}
